<?xml version="1.0" encoding="UTF-8"?>
<configurations>
  <configuration>
    <name>cm</name>
    <description>Configuration for the Collective Media HBase cluster.</description>
    <hadoop_master>tr-bt-dal-01.collective-media.net</hadoop_master>
    <hadoop_job_tracker>tr-bt-dal-01.collective-media.net</hadoop_job_tracker>
    <hbase_master>tr-bt-dal-01.collective-media.net</hbase_master>
    <schema>
      <table>
        <name>docs</name>
        <table_name>ma-docs</table_name>
        <description>Stores the actual documents.</description>
        <column_family>
          <name>titles</name>
          <description>Holds the titles of the documents.</description>
          <!-- Default: 3 -->
          <max_versions/>
          <!-- Default: DEFAULT_COMPRESSION_TYPE -->
          <compression_type/>
          <!-- Default: false -->
          <in_memory/>
          <!-- Default: false -->
          <block_cache_enabled/>
          <!-- Default: -1 (forever) -->
          <time_to_live/>
          <!-- Default: 2147483647 -->
          <max_value_length/>
          <!-- Default: DEFAULT_BLOOM_FILTER_DESCRIPTOR -->
          <bloom_filter/>
        </column_family>
        <column_family>
          <name>contents</name>
          <description>Holds the actual raw data.</description>
          <!-- Default: 3 -->
          <max_versions/>
          <!-- Default: DEFAULT_COMPRESSION_TYPE -->
          <compression_type/>
          <!-- Default: false -->
          <in_memory/>
          <!-- Default: false -->
          <block_cache_enabled/>
          <!-- Default: -1 (forever) -->
          <time_to_live/>
          <!-- Default: 2147483647 -->
          <max_value_length/>
          <!-- Default: DEFAULT_BLOOM_FILTER_DESCRIPTOR -->
          <bloom_filter/>
        </column_family>
        <column_family>
          <name>mimetype</name>
          <description>Holds the MIME type of the data.</description>
          <!-- Default: 3 -->
          <max_versions/>
          <!-- Default: DEFAULT_COMPRESSION_TYPE -->
          <compression_type/>
          <!-- Default: false -->
          <in_memory/>
          <!-- Default: false -->
          <block_cache_enabled/>
          <!-- Default: -1 (forever) -->
          <time_to_live/>
          <!-- Default: 2147483647 -->
          <max_value_length/>
          <!-- Default: DEFAULT_BLOOM_FILTER_DESCRIPTOR -->
          <bloom_filter/>
        </column_family>
        <column_family>
          <name>language</name>
          <description>Holds the original language of the data.</description>
          <!-- Default: 3 -->
          <max_versions/>
          <!-- Default: DEFAULT_COMPRESSION_TYPE -->
          <compression_type/>
          <!-- Default: false -->
          <in_memory/>
          <!-- Default: false -->
          <block_cache_enabled/>
          <!-- Default: -1 (forever) -->
          <time_to_live/>
          <!-- Default: 2147483647 -->
          <max_value_length/>
          <!-- Default: DEFAULT_BLOOM_FILTER_DESCRIPTOR -->
          <bloom_filter/>
        </column_family>
        <column_family>
          <name>cache</name>
          <description>Holds the transformed versions of the data.</description>
          <!-- Default: 3 -->
          <max_versions/>
          <!-- Default: DEFAULT_COMPRESSION_TYPE -->
          <compression_type/>
          <!-- Default: false -->
          <in_memory/>
          <!-- Default: false -->
          <block_cache_enabled/>
          <!-- Default: -1 (forever) -->
          <time_to_live/>
          <!-- Default: 2147483647 -->
          <max_value_length/>
          <!-- Default: DEFAULT_BLOOM_FILTER_DESCRIPTOR -->
          <bloom_filter/>
        </column_family>
        <column_family>
          <name>domain</name>
          <description>Holds the domain information of the data.</description>
          <!-- Default: 3 -->
          <max_versions/>
          <!-- Default: DEFAULT_COMPRESSION_TYPE -->
          <compression_type/>
          <!-- Default: false -->
          <in_memory/>
          <!-- Default: false -->
          <block_cache_enabled/>
          <!-- Default: -1 (forever) -->
          <time_to_live/>
          <!-- Default: 2147483647 -->
          <max_value_length/>
          <!-- Default: DEFAULT_BLOOM_FILTER_DESCRIPTOR -->
          <bloom_filter/>
        </column_family>
      </table>
      <table>
        <name>trans</name>
        <table_name>ma-trans</table_name>
        <description>Stores the transformations.</description>
        <column_family>
          <name>contents</name>
          <description>Holds the actual raw data.</description>
          <!-- Default: 3 -->
          <max_versions/>
          <!-- Default: DEFAULT_COMPRESSION_TYPE -->
          <compression_type/>
          <!-- Default: false -->
          <in_memory/>
          <!-- Default: false -->
          <block_cache_enabled/>
          <!-- Default: -1 (forever) -->
          <time_to_live/>
          <!-- Default: 2147483647 -->
          <max_value_length/>
          <!-- Default: DEFAULT_BLOOM_FILTER_DESCRIPTOR -->
          <bloom_filter/>
        </column_family>
        <column_family>
          <name>mimetype</name>
          <description>Holds the MIME type of the transformation.</description>
          <!-- Default: 3 -->
          <max_versions/>
          <!-- Default: DEFAULT_COMPRESSION_TYPE -->
          <compression_type/>
          <!-- Default: false -->
          <in_memory/>
          <!-- Default: false -->
          <block_cache_enabled/>
          <!-- Default: -1 (forever) -->
          <time_to_live/>
          <!-- Default: 2147483647 -->
          <max_value_length/>
          <!-- Default: DEFAULT_BLOOM_FILTER_DESCRIPTOR -->
          <bloom_filter/>
        </column_family>
      </table>
      <table>
        <name>lookup</name>
        <table_name>ma-lookup</table_name>
        <description>Stores the lookup details.</description>
        <column_family>
          <name>fullkey</name>
          <description>Holds the actual full key.</description>
          <!-- Default: 3 -->
          <max_versions></max_versions>
          <!-- Default: DEFAULT_COMPRESSION_TYPE -->
          <compression_type></compression_type>
          <!-- Default: false -->
          <in_memory></in_memory>
          <!-- Default: false -->
          <block_cache_enabled></block_cache_enabled>
          <!-- Default: -1 (forever) -->
          <time_to_live></time_to_live>
          <!-- Default: 2147483647 -->
          <max_value_length></max_value_length>
          <!-- Default: DEFAULT_BLOOM_FILTER_DESCRIPTOR -->
          <bloom_filter></bloom_filter>
        </column_family>
      </table>
      <table>
        <name>index-sorted</name>
        <table_name>ma-index-sorted</table_name>
        <description>Stores the sorted index details.</description>
        <column_family>
          <name>items</name>
          <description>Holds a block of index items.</description>
          <!-- Default: 3 -->
          <max_versions></max_versions>
          <!-- Default: DEFAULT_COMPRESSION_TYPE -->
          <compression_type></compression_type>
          <!-- Default: false -->
          <in_memory></in_memory>
          <!-- Default: false -->
          <block_cache_enabled></block_cache_enabled>
          <!-- Default: -1 (forever) -->
          <time_to_live></time_to_live>
          <!-- Default: 2147483647 -->
          <max_value_length></max_value_length>
          <!-- Default: DEFAULT_BLOOM_FILTER_DESCRIPTOR -->
          <bloom_filter></bloom_filter>
        </column_family>
      </table>
      <table>
        <name>index-codes</name>
        <table_name>ma-index-codes</table_name>
        <description>Stores the available codes for the index.</description>
        <column_family>
          <name>total</name>
          <description>Holds the total number of titles per code.</description>
          <!-- Default: 3 -->
          <max_versions></max_versions>
          <!-- Default: DEFAULT_COMPRESSION_TYPE -->
          <compression_type></compression_type>
          <!-- Default: false -->
          <in_memory></in_memory>
          <!-- Default: false -->
          <block_cache_enabled></block_cache_enabled>
          <!-- Default: -1 (forever) -->
          <time_to_live></time_to_live>
          <!-- Default: 2147483647 -->
          <max_value_length></max_value_length>
          <!-- Default: DEFAULT_BLOOM_FILTER_DESCRIPTOR -->
          <bloom_filter></bloom_filter>
        </column_family>
        <column_family>
          <name>blocks</name>
          <description>Holds the number of blocks (of titles) per code.</description>
          <!-- Default: 3 -->
          <max_versions></max_versions>
          <!-- Default: DEFAULT_COMPRESSION_TYPE -->
          <compression_type></compression_type>
          <!-- Default: false -->
          <in_memory></in_memory>
          <!-- Default: false -->
          <block_cache_enabled></block_cache_enabled>
          <!-- Default: -1 (forever) -->
          <time_to_live></time_to_live>
          <!-- Default: 2147483647 -->
          <max_value_length></max_value_length>
          <!-- Default: DEFAULT_BLOOM_FILTER_DESCRIPTOR -->
          <bloom_filter></bloom_filter>
        </column_family>
      </table>
      <table>
        <name>meta</name>
        <table_name>ma-meta</table_name>
        <description>Stores meta information (counts, updates etc.).</description>
        <column_family>
          <name>counts</name>
          <description>Holds the data counts.</description>
          <!-- Default: 3 -->
          <max_versions/>
          <!-- Default: DEFAULT_COMPRESSION_TYPE -->
          <compression_type/>
          <!-- Default: false -->
          <in_memory/>
          <!-- Default: false -->
          <block_cache_enabled/>
          <!-- Default: -1 (forever) -->
          <time_to_live/>
          <!-- Default: 2147483647 -->
          <max_value_length/>
          <!-- Default: DEFAULT_BLOOM_FILTER_DESCRIPTOR -->
          <bloom_filter/>
        </column_family>
        <column_family>
          <name>updates</name>
          <description>Holds keys of the updated/changed rows.</description>
          <!-- Default: 3 -->
          <max_versions/>
          <!-- Default: DEFAULT_COMPRESSION_TYPE -->
          <compression_type/>
          <!-- Default: false -->
          <in_memory/>
          <!-- Default: false -->
          <block_cache_enabled/>
          <!-- Default: -1 (forever) -->
          <time_to_live/>
          <!-- Default: 2147483647 -->
          <max_value_length/>
          <!-- Default: DEFAULT_BLOOM_FILTER_DESCRIPTOR -->
          <bloom_filter/>
        </column_family>
        <column_family>
          <name>indexes</name>
          <description>Holds the index details after each full index run.</description>
          <!-- Default: 3 -->
          <max_versions/>
          <!-- Default: DEFAULT_COMPRESSION_TYPE -->
          <compression_type/>
          <!-- Default: false -->
          <in_memory/>
          <!-- Default: false -->
          <block_cache_enabled/>
          <!-- Default: -1 (forever) -->
          <time_to_live/>
          <!-- Default: 2147483647 -->
          <max_value_length/>
          <!-- Default: DEFAULT_BLOOM_FILTER_DESCRIPTOR -->
          <bloom_filter/>
        </column_family>
      </table>
    </schema>
  </configuration>
</configurations>